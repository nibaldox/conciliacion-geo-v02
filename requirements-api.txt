fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.0
python-multipart>=0.0.12
numpy>=1.26
scipy>=1.13
trimesh>=4.5
fast_simplification>=0.1.13
shapely>=2.0
openpyxl>=3.1.5
plotly>=5.24
python-docx>=1.1
ezdxf>=1.3.0
matplotlib>=3.9
reportlab>=4.0
openai>=1.42
pandas>=2.2
# Production middleware (opt-in via env vars — safe to install even
# when disabled, since the middleware module no-ops in that case).
slowapi>=0.1.9
# Observability (opt-in via SENTRY_DSN — no-op when unset).
sentry-sdk[fastapi]>=2.0
networkx>=3.0
